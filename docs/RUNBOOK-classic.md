# Classic orch (archived)

The classic REPL/app-server based orch is preserved on the `orch-classic` branch.

To work on it side-by-side:

```bash
git worktree add ../orch-classic-wt orch-classic
```
